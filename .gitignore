# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
# prevents the node_modules directory from being tracked, which contains all the dependencies installed via npm or yarn. These can be large and are not necessary to include in the repository since they can be installed via npm install or yarn install.
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
